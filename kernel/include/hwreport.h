#ifndef TUNIX_HWREPORT_H
#define TUNIX_HWREPORT_H

/* What this machine turned out to be, printed and written to the root
   filesystem when `hwreport` is on the command line. */
void hwreport_emit(void);

#endif
